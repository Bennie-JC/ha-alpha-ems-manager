# Test tooling for the Alpha EMS Manager integration.
#
# pytest-homeassistant-custom-component pulls in a matching Home Assistant
# release plus pytest and the test helpers used by the HA test suite.
# Both are pinned so CI and a local checkout apply exactly the same rules; an
# unpinned Ruff turns a new release into a surprise CI failure on an unrelated PR.
ruff==0.16.1
pytest-homeassistant-custom-component==0.13.316
