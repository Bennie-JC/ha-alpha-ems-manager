[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = .
# tests.win_compat must load before the pytest-homeassistant-custom-component
# entry-point plugin; see the docstring in tests/win_compat.py. It is inert on
# Linux and macOS.
addopts = -p no:cacheprovider -p tests.win_compat
